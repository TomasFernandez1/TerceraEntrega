<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    {{#each products}}
      <strong><h3>{{this.title}}</h3></strong>
      <h4>${{this.price}}</h4>
      <h4>{{this.quantity}}</h4>
    {{/each}}
    <form action="/api/carts/{{req.user.cart}}/purchase" method="post"><button type="submit">Purchase</button></form>

  </body>
</html>